# SPS — KERNEL CONSTITUTION
## نظام المعالجة السيادي — الدستور المعماري

**الإصدار:** 3.1.0  
**الحالة:** ملزم — لا يُعدَّل إلا بقرار معماري موثق

---

> هذه الوثيقة ليست توصية. هي القانون الذي يحكم كل ملف، كل دالة، كل قرار معماري داخل SPS.

## المبادئ المطلقة
- **Zone A** — 100% Deterministic
- **الأحداث** — Append-only, لا تُعدَّل ولا تُحذف
- **Replay** — `replay(events[0..n]) == state_at_event_n` إلى الأبد
- **الدليل** — كل استنتاج يحمل EvidenceLevel
- **التطور** — لا يُعدَّل Zone A أبداً، ولا يُلغى Replay Validator

[النص الكامل للدستور مُتاح في الملف الأصلي]
